# Task: Make pares-modulus main CI green

## Context
- Repo: C:\Projects\pares-modulus (plures/pares-modulus, active plugin collection for pares-radix)\n- main CI has been RED since 2026-05-08. Root causes found & partially fixed by orchestrator:
  1. FIXED: pnpm-lock.yaml was never committed + bogus `allowBuilds:` stub in pnpm-workspace.yaml
     jammed lockfile generation. Removed the stub; regenerated a valid 50.7KB pnpm-lock.yaml.
  2. FIXED: eslint.config.js registered the local `plures` eslint plugin only for `src/**`
     (which doesn't exist) causing 38 "rule not found" errors, and NOT enabling the
     architectural rules on plugins was intentional (plugin UIs use Svelte 5 runes at the
     view layer and already carry `eslint-disable plures/no-raw-stores` comments). Fixed by
     registering the plugin globally (rules OFF) + keeping rules enabled only for src/**.
- REMAINING: 27 genuine lint ERRORS + 44 warnings. CI lint step runs `pnpm run lint`
  (= `eslint plugins/ gates/ scripts/`); ESLint fails on errors only (no --max-warnings).

## Do NOT
- Do NOT re-enable plures/no-raw-stores on plugins/** (design intent: OFF there).
- Do NOT touch the org reusable workflow (plures/.github).
- Do NOT add stubs, `any` casts to silence, or `// eslint-disable` to hide real errors.
  Fix them for real (C-NOSTUB-001). Removing genuinely-dead code/CSS/vars is fine.

## The 27 errors to fix (run `pnpm run lint` for exact file:line)
- 2x prefer-const (`line`, `rawVal`) — auto-fixable via `pnpm exec eslint --fix`
- ~8x no-unused-vars: ModelChunkEvent, ScanState, autoRefresh, refreshInterval,
  VaultSetPayload, formPassword, etc. — remove if truly unused, or `_`-prefix if kept for intent.
- 4x no-explicit-any — give real types (inspect the value; do not cast to `unknown` blindly).
- 6x "Unused CSS selector" (.help-toggle, .help-toggle:hover, .add-form-card, .form-errors p,
  button.tx-table__th, button.tx-table__th:hover) — remove the dead CSS OR wire the selector
  to the markup if the element was meant to have that class (check intent first).
- ~5x "This type of directive is not valid on components" — a Svelte directive
  (transition:/animate:/use:/class: etc.) is applied to a Component instead of a DOM element.
  Wrap in an element or move the directive to the right element. Inspect each.
- 2x "Parsing error: '>' expected" (both at col 71) — real Svelte template syntax bug.
  Open the exact line; likely a malformed generic `<T>` in markup or a broken tag. Fix the syntax.
- 2x a11y on a `<div>` with click handler (needs keyboard handler + ARIA role) —
  prefer converting to `<button type="button">`, else add role + on:keydown.

## Also (warnings cleanup — do after errors are green)
- Remove now-dead `// eslint-disable-next-line plures/no-raw-stores` comments in plugin files
  (they're "Unused eslint-disable directive" warnings — the rule no longer runs there).
- Remove the 6 unused `no-console` disable directives flagged as warnings.

## HARD GATE (must pass before reporting done)
1. `pnpm install --frozen-lockfile` exits 0
2. `pnpm run lint` exits 0 (zero errors; aim for zero warnings too)
3. `pnpm run build` (tsc --noEmit) exits 0
Record each gate result in .task-ci-greenup-milestones.md as you go.

## Commit
- One clean commit on a task branch. Message:
  `fix(ci): commit pnpm-lock.yaml, scope plures eslint plugin, clear lint debt (green main)`
- Include: pnpm-workspace.yaml (stub removed), pnpm-lock.yaml (new), eslint.config.js (plugin
  scoping), removal of package-lock.json (conflicting npm lockfile — pnpm is the PM), and all
  the plugin source fixes.
- Do NOT push. Report back with the branch name + gate results; orchestrator handles push/PR.
