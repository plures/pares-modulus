packages:
  - "packages/*"

# esbuild ships the native binary used by tsx. pnpm 11 blocks unlisted build
# scripts; without this explicit allow-list it cannot execute registry gates.
allowBuilds:
  esbuild: true

# Preserve compatibility with the pnpm 10 config used by existing runners.
onlyBuiltDependencies:
  - esbuild
